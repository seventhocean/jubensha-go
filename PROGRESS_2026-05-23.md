# 进度记录 — 2026-05-23

## 本日提交

### ebcf5e35 — Cloudflare R2 独立上传 + 群组 UX 修复
- 新增 Cloudflare R2 上传后端（独立于 AWS S3），dto、uploader、service、dashboard 完整链路
- 修复上传图片响应解析：`data.code` → `data.errorCode`，`data.msg` → `data.message`
- 修复图片上传损坏：改为始终 `io.ReadAll` 完整读取，避免 Content-Length 不匹配
- slugify 改进：中文名自动生成 fallback slug（`group-` + 6位随机字符）
- 修复群组详情页 trailing slash 导致空白页（`isChildRoute` 改用 `startsWith`）
- 群组发帖按钮加入成员身份校验，未加入提示"请先加入群组，才能发帖"
- 群组签到加入成员身份校验，未加入提示"请先加入群组，才能签到"
- 退出群组增加 AlertDialog 确认弹窗
- 群组卡片恢复"X 成员 · Y 帖子"文字标签，手机端 1 列布局

### 4a5e498e — Favicon
- `root.tsx` 中复用 `siteLogo` 作为浏览器标签页图标

### b88d4022 — 移动端 UX 重构 + 群组卡片 + 导航优化
- 底部导航改为 5 tab：话题 / 文章 / 发布(+) / 群组 / 任务
- 移动端 Header 加搜索🔍 + 通知🔔 图标按钮
- 精简汉堡菜单：移除搜索和发布（已在外层可直接访问）
- 群组卡片缩小为 4 列桌面布局（手机 1 列、平板 2 列）
- 左侧导航群组列表与节点左对齐（去除 px-3 包裹）
- 内置节点（最新/推荐/关注）加 lucide 图标：Clock / Flame / Rss

## 远端拉取的优化（07d9e0fa）
- 导航预加载：Link 组件默认 `prefetch="intent"`
- shouldRevalidate 避免不必要 API 请求
- 图片全量 `loading="lazy"` + `decoding="async"`
- 关注按钮乐观更新

## 设计系统全面落地（d866e955 之前）
- DESIGN.md 重写，variables.css 重构
- 全部组件硬编码色值替换为设计 token
- 新增 mobile-bottom-nav 组件
- 引入 Inter + JetBrains Mono 字体

## 涉及文件（本日个人提交）
| 文件 | 变更类型 |
|------|---------|
| `internal/pkg/uploader/r2_uploader.go` | 新增 |
| `internal/models/dto/config_dto.go` | 新增 R2UploadConfig, R2 常量 |
| `internal/services/upload_service.go` | 新增 R2 上传分支 |
| `internal/handlers/api/upload_handlers.go` | 修复文件读取方式 |
| `internal/pkg/uploader/aws_s3_uploader.go` | 回退 endpoint 改动 |
| `web/app/root.tsx` | favicon |
| `web/app/routes/groups._index.tsx` | 卡片布局、退出弹窗、标签文字 |
| `web/app/routes/groups.$slug.tsx` | 发帖校验、签到校验、退出弹窗、路由修复 |
| `web/app/routes/groups.$slug.settings.tsx` | 上传响应解析修复 |
| `web/app/routes/groups.create.tsx` | slugify 修复、上传响应解析 |
| `web/components/layout/mobile-bottom-nav.tsx` | 5 tab 重设计 |
| `web/components/layout/site-header.tsx` | 移动端搜索+通知图标 |
| `web/components/topic/topics-nav-content.tsx` | 群组对齐 + 内置节点图标 |
| `web/components/common/skeleton-list.tsx` | 骨架屏匹配新布局 |
| `web/lib/i18n/messages/zh-CN.ts` | 多项 i18n 新增 |
| `web/lib/i18n/messages/en-US.ts` | 多项 i18n 新增 |
